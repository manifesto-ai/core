{
  "name": "@manifesto-ai/translator-playground",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:deps": "pnpm -C ../../packages/intent-ir build && pnpm -C ../../packages/translator/core build && pnpm -C ../../packages/translator/adapters/translator-adapter-openai build && pnpm -C ../../packages/translator/targets/translator-target-manifesto build",
    "play": "pnpm run build:deps && node ./src/playground.mjs",
    "test:llm": "pnpm run build:deps && vitest run"
  },
  "dependencies": {
    "@manifesto-ai/intent-ir": "workspace:*",
    "@manifesto-ai/translator": "workspace:*",
    "@manifesto-ai/translator-adapter-openai": "workspace:*",
    "@manifesto-ai/translator-target-manifesto": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^4.0.18"
  }
}
