# Use npm registry for @manifesto-ai packages
@manifesto-ai:registry=https://registry.npmjs.org/

# Enable strict peer dependencies
strict-peer-dependencies=false

# For publishing: ensure logged in to npm
# Run: npm login --registry=https://registry.npmjs.org/ --scope=@manifesto-ai
